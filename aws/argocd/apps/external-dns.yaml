# Auto-generated file, do not edit directly
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  annotations:
    argocd.argoproj.io/sync-wave: "-8"
  name: external-dns
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: external-dns
    server: https://kubernetes.default.svc
  project: default
  sources:
    - repoURL: https://kubernetes-sigs.github.io/external-dns/
      targetRevision: 1.20.0
      chart: external-dns
      helm:
        releaseName: external-dns
        valuesObject:
          sources:
            - gateway-httproute
          provider:
            name: cloudflare
          domainFilters:
            - haigh.cloud
          policy: sync
          txtOwnerId: certmanager
          env:
            - name: CF_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: cloudflare-api-token
                  key: api-token
    - repoURL: https://github.com/michaelhaigh/kasten-automation
      targetRevision: certmanager
      path: aws/argocd/addons/external-dns
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true
